# Канал о смыслах в моде

Собирает мировую модную ленту (индустрия, производство, культура), находит сюжеты, в которых частность открывает смысл, пишет пост в голосе канала и присылает черновик вам в Telegram. Публикуется только то, что вы одобрили.

```
RSS и карты сайтов (30 источников, включая трендовые агентства) → дедуп и кластеры → отбор (Haiku 4.5) → смысл (Opus 5) → текст (Opus 5)
   → черновик вам в бота → ✅ очередь / 🚀 сейчас / ✏️ правка / ⏸ / ✖️ → канал по расписанию
```

## Запуск

1. **Бот.** В Telegram откройте @BotFather → `/newbot` → получите токен.
2. **Канал.** Добавьте бота в канал администратором с правом «Публикация сообщений».
3. **Настройки.** `cp .env.example .env`, заполните `DATABASE_URL`, `ANTHROPIC_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHANNEL_ID` (`@username` канала).
4. **База и зависимости.**
   ```bash
   npm install
   createdb channel
   ```
5. **Ваш id.** `npm start`, напишите боту `/start` — он пришлёт chat id. Впишите его в `TELEGRAM_OWNER_CHAT_ID`, перезапустите.

После этого бот сам собирает ленту каждые 6 часов и присылает до 3 черновиков.

Посмотреть, что попадает в отбор, без генерации постов: `npm run collect`.

## Что настраивать в первую очередь

| Что | Где |
|---|---|
| Голос канала, структура, запреты | `style/guide.md` |
| **Образцы ваших постов** (сильнее всего влияют на качество) | `style/examples/01.md`, `02.md`… |
| Линзы и логика «частность → смысл» | `src/prompts.js` |
| Источники | `src/sources.js` |
| Слоган в конце каждого поста | `style/signature.txt` (пустой файл — без слогана) |
| Частота, порог оценки, часы публикации | `.env` |

## Форматы по дням — цикл на две недели

У каждого дня свой формат поста, у каждого формата своя структура и свои образцы (`src/formats.js`).

| | Неделя 1 | Неделя 2 |
|---|---|---|
| Пн | Слова тренда | Сигнал прогноза |
| Вт | Материал и производство | Цвет |
| Ср | Одна вещь — разные смыслы | Кампейн по слоям |
| Чт | Теория на примере | Одно слово |
| Пт | Как менялся код | Сигналы статуса |
| Сб | Психология стиля | Тело и норма |
| Вс | Приёмы | Улица и подиум |

Неделя 1 началась 14 сентября 2026 (`CYCLE_START`), дальше недели чередуются. Черновики готовятся в формате дня ближайшего слота публикации (вечером — уже завтрашнего). В слот публикуется одобренный пост формата дня, а если такого нет — самый старый одобренный.

## Чистка от ИИ-слопа

`src/slop.js` — детектор шаблонов машинного текста в русском: пустые конструкции («в современном быстро меняющемся мире», «играет ключевую роль»), шаблонные начала («Более того», «По сути»), заполнители («действительно», «невероятно»), абсолютные обобщения, «Это не X. Это Y», больше одной антитезы «не X, а Y». Невидимые символы (нулевой ширины, мягкий перенос, метки направления) удаляются кодом. Если после литредактора детектор что-то нашёл, модель точечно переписывает именно эти места; что осталось — видно в черновике строкой «⚠️ шаблоны: …». Слова код сам не вырезает — это ломает грамматику. `/check` (кнопка «🧹 Проверить на слоп») проверяет очередь и неутверждённые черновики; «🧹 Вычистить #N» точечно переписывает найденные места и присылает новую версию на утверждение (пост из очереди до этого снимается).

## Память правок («Уроки редактора»)

После каждого комментария к правке Opus решает, есть ли в нём общее правило («не начинай с цифры», «вопрос читателю — одной фразой») или это правка только этого поста. Общие правила хранятся в `editor_rules` и подставляются в промпты написания и литредактуры — каждый следующий черновик их учитывает. Бот сообщает «🧠 Запомнено: …» с кнопкой «Не запоминать»; `/rules` — список с удалением, `/rule текст` — добавить вручную. Не больше 25 правил: при превышении список сжимается.

## Картинки из игры

Раз в сутки канал забирает каталог образов FFE через открытый API игры (`FFE_API_URL`, по умолчанию `ffe-production.up.railway.app/api`) — картинки в Cloudinary и описания по пяти слоям. После написания поста Haiku ищет образ, перекликающийся с мыслью поста; картинка ставится только при хорошем совпадении (fit ≥ 4), поэтому часть постов выходит без неё. Образ не повторяется в последних 10 постах. Картинка идёт крупным превью над текстом (подпись к фото в Telegram ограничена 1024 знаками). В черновике есть кнопка «🖼 Убрать картинку».

## Закрытость

Бот работает только для `TELEGRAM_OWNER_CHAT_ID`: посторонним не отвечает (подсказка с chat id — только пока владелец не задан), нажатия чужих кнопок игнорирует, меню команд видно только владельцу. Если бота добавят в чужую группу или канал, он выходит оттуда. Владельцу приходит «🔒 Посторонний … написал(а) боту» — не чаще раза в сутки на человека. В @BotFather стоит выключить добавление в группы: `/setjoingroups` → Disable.

## Команды бота

Кнопка «Меню» у поля ввода (видна только владельцу) и `/menu` с кнопками:

`/status` — сводка: неделя и формат дня, ближайшая публикация, очередь, ждут решения · `/run` — прогон сейчас (идёт в фоне) · `/pending` — «Не утверждено»: список черновиков с кнопками «Открыть #N» и «Прислать все» · `/queue` — очередь · `/deferred` — вернуть отложенные · `/formats` — расписание на две недели · `/cancel` — отменить правку.

Если черновик ждёт решения больше 2 часов, бот напоминает раз в 4 часа (ответом на самый старый черновик), а за час до слота при пустой очереди — срочно. С 22 до 9 бот молчит. Настраивается `REMIND_EVERY_HOURS` и `QUIET_HOURS`.

«✏️ Править» → бот отвечает на черновик вопросом с его заголовком → напишите одним сообщением, что изменить → придёт новая версия. Или просто ответьте на черновик («Ответить»/свайп) текстом правки.

## Как устроено

- `collect.js` — RSS, карты сайтов (`type: 'sitemap'` — Jing Daily, Peclers Paris) и публичные Telegram-каналы через `t.me/s/…` (`type: 'telegram'` — BeInOpen, The Blueprint, РБК Стиль, The Symbol); трендовым агентствам окно 30 дней; закрытые издания (BoF, Vogue Business, FashionUnited, Ecotextile) — через Google News с фильтром по домену. Вакансии и гороскопы отсекаются.
- `cluster.js` — одна новость из разных изданий склеивается в сюжет (пересечение слов заголовков). Сюжет из нескольких источников и сюжеты из производственного слоя получают бонус при отборе.
- `pipeline.js` — отбор → разбор (`fact / detail / shift / frame / general / reader_turn / thesis`) → текст. Разбор хранится отдельно от текста: правка переписывает текст, не пересобирая смысл.
- Память: последние 40 тезисов идут в промпт разбора — модель отбрасывает повторы. Линзы трёх последних постов штрафуются, чтобы канал не зацикливался.
- Ссылки на источники дописываются кодом, а не моделью: они не могут быть выдуманы.
- Для Opus 5 включён серверный fallback (`fallbacks: "default"`): при ложном отказе классификатора запрос сам перезапускается на другой модели.

## Что дальше

- Эмбеддинги вместо пересечения слов — точнее склеивать сюжеты и ловить смысловые повторы.
- Telegram-каналы как источники — через клиентский API (GramJS), бот их читать не может.
- Обложка: типографический шаблон в Cloudinary с тезисом поста.
- Просмотры и реакции → раз в месяц лучшие посты уходят в `style/examples`.
